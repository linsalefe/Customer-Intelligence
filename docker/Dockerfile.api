FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ src/
COPY config/ config/
COPY sql/ sql/

CMD ["uvicorn", "src.webhook_api.app:app", "--host", "0.0.0.0", "--port", "8001"]
